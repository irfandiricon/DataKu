DELIMITER $$

USE `ircm7344_dataku`$$

DROP TRIGGER /*!50032 IF EXISTS */ `AI_CUSTOMER`$$

CREATE
    /*!50017 DEFINER = 'root'@'localhost' */
    TRIGGER `AI_CUSTOMER` AFTER INSERT ON `customer` 
    FOR EACH ROW BEGIN
	INSERT INTO CUSTOMER_DEPOSITE (EMAIL) VALUES (NEW.EMAIL);
    END;
$$

DELIMITER ;