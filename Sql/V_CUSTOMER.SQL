
CREATE VIEW `pusb8564_dataku`.`V_CUSTOMER` 
    AS
(SELECT A.ID, A.NAME, A.EMAIL, A.PASSWORD, A.NO_HP, A.IS_AGEN, A.REFERRAL_ID, A.PIN, A.FILE_PROFILE, A.STATUS,
A.REGISTERED_BY, B.SALDO
FROM CUSTOMER AS A
LEFT JOIN CUSTOMER_DEPOSITE AS B ON A.EMAIL = B.EMAIL);
